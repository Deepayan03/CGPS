## Steps to run the backend
1. cd backend
2. npm install
3. npm run dev(for directly running the ts project)
4. npm run build (for compiling ts to js)
5. npm start (for running the compiled project from the dist directory)
